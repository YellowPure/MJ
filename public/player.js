function Player(){
	this.card_list=[];
	this.player_view=new createjs.Container();
}
Player.prototype.initCardsAndPos=function(pos){
	this.card_list=list;
	for (var index = 0; index < this.card_list.length; index++) {
		var card=new Card({
			
		})
		createjs.Tween.get(element).to({
			
		})
	}
	
	console.log('init player cards',list);
}